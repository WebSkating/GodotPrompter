---
type: llm
focus: last_message
weight: 2
---
Pass only if the answer states: _physics_process runs at a fixed rate tied to the physics tick (default 60 per second, so delta is effectively constant); _process runs once per rendered frame with a variable delta; and it gives the usage rule — physics/movement/collision code in _physics_process, visuals/UI/non-physics logic in _process.
